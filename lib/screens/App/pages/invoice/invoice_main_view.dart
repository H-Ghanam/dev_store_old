import 'package:fluent_ui/fluent_ui.dart';

class InvoiceMainView extends StatelessWidget {
  const InvoiceMainView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Card(
        child: Button(
      child: const Text("ابدأ من هنا"),
      onPressed: () {},
    ));
  }
}
